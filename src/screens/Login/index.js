import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Button, Fields} from '../../component'

const Login = () => {
  return (
    <View>
      <Button />
    </View>
  )
}

export default Login

const styles = StyleSheet.create({})